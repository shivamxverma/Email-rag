services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: mount data for local edits (comment out to use image copy)
    # volumes:
    #   - ./data:/app/data:ro
